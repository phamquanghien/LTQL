@model IEnumerable<LTQL.Models.Product>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Customers/Views/Shared/_LayoutCustomer.cshtml";
}
<br />
<div class="w3-row-padding w3-margin-top">
    @foreach (var item in Model)
    {
        @*<div class="w3-padding w3-round-large w3-hover-border-blue col-lg-3 w3-hover-border-blue">
                <img class="w3-round-large" src="~/Upload/ProductImage/Vay1.jpg" style="width:100%" />
                <div class="w3-container">
                    <h4>@Html.DisplayFor(modelItem => item.ProductName)</h4>
                </div>
            </div>*@
        <div class="w3-third w3-margin-bottom">
            <div class="w3-card w3-round-large w3-padding w3-border w3-hover-border-blue">
                <img class="w3-round-large" src="~/Upload/ProductImage/Vay1.jpg" style="width:100%" />
                <h4>@Html.DisplayFor(modelItem => item.ProductName)</h4>
                <h4>$@Html.DisplayFor(modelItem => item.Price)</h4>
            </div>
        </div>
    }
</div>